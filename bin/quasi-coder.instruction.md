# Bin Instructions

This folder will hold:

- Scripts executed by the command-line tool `sendEmail`
  - Script language or file-type should vary and account for the OS:
    - Windows
    - Linux
    - MacOS
- Each script must be able to easily integrate or be plugged into an external API i.e. VS Code extension or a GUI library
