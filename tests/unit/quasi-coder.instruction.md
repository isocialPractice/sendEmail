# Tests `test/unit` Instructions

This folder will hold:

- Unit tests for the `sendEmail` tool that will be specified from the argument passed to `--test` option
  - If no argument is passed to `--test`, then all unit test will run in a sequence
